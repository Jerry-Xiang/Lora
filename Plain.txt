Case_lora/
├── app.py                    # 主入口文件（内存检查、日志初始化、流水线启动）
├── config.yaml               # 配置文件（模型、LoRA、训练、数据、推理配置）
├── requirements.txt          # 依赖包版本（pip install -r requirements.txt）
├── ARCHITECTURE.md           # 架构文档（模块说明、依赖关系、冲突解决方案）
├── test_train.py             # 独立训练测试脚本（transformers+peft架构验证）
├── inference_lora.py         # LoRA增量推理脚本（使用微调后模型推理）
├── merge_lora.py             # LoRA与基座合并权重脚本（生成完整微调模型）
├── data/                     # 数据目录
│   ├── qwen-vl-train.xlsx    # 训练数据（Excel格式）
│   └── images/               # 测试图片目录
│       ├── 1-vehicle-odometer-reading.jpg
│       └── 2-vehicle-odometer-reading.jpg
├── models/                   # 模型缓存目录
│   └── qwen/
│       └── Qwen2___5-VL-3B-Instruct/  # Qwen2.5-VL-3B-Instruct模型文件
├── outputs/                  # 训练输出目录（检查点）
├── car_insurance_lora_model/ # LoRA适配器保存目录（训练完成后生成）
├── logs/                     # 日志目录
│   └── training_log_*.txt    # 训练日志文件（按时间命名）
└── src/                      # 源码目录
    ├── __init__.py
    ├── logger.py             # 日志记录器（同时输出到终端和文件）
    ├── config_factory.py     # 配置工厂（加载和管理配置参数）
    ├── model_factory.py      # 模型工厂（下载、加载、LoRA配置）
    ├── data_factory.py       # 数据工厂（训练数据加载和预处理）
    ├── training_factory.py   # 训练工厂（手动训练循环、模型保存）
    ├── inference_factory.py  # 推理工厂（训练前后推理测试）
    └── pipeline.py           # 训练流水线（主控制器，Step 1-8流程）
